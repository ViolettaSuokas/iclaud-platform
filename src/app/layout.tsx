import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Crab Cloud - AI Agent Platform",
  description: "Deploy, manage, and scale your AI agents in the cloud. Build powerful agent chains with enterprise-grade security.",
  keywords: ["AI agents", "cloud deployment", "agent chains", "machine learning", "automation"],
  authors: [{ name: "Crab Cloud" }],
  openGraph: {
    title: "Crab Cloud - AI Agent Platform",
    description: "Deploy, manage, and scale your AI agents in the cloud.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
